<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageButton
        android:id="@+id/toolbar_bell_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/ic_notification"
        android:tint="#000000"
        />

    <androidx.cardview.widget.CardView
        android:id="@+id/toolbar_bell_number_container"
        android:layout_width="14dp"
        android:layout_height="14dp"
        app:cardBackgroundColor="@color/colorPrimaryLight"
        app:layout_constraintEnd_toEndOf="@id/toolbar_bell_icon"
        app:layout_constraintTop_toTopOf="@id/toolbar_bell_icon"
        app:cardCornerRadius="50dp"
        >
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <TextView
                android:id="@+id/toolbar_bell_number_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_centerVertical="true"
                android:textSize="10sp"
                android:text="1"
                android:textColor="#000" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</androidx.constraintlayout.widget.ConstraintLayout>